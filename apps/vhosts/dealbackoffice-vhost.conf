<VirtualHost *:80>
    ServerAdmin a2zstreetdjs@gmail.com
    ServerName backoffice.techyline.com
    DocumentRoot /opt/bitnami/apache/apps/backoffice/public

    # Redirect HTTP to HTTPS while preserving POST requests
    RewriteEngine On
    RewriteCond %{HTTPS} !=on
    RewriteRule ^/(.*)$ https://backoffice.techyline.com/$1 [R=301,L]
</VirtualHost>