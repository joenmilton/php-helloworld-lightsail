name: Deploy to Lightsail

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: dev
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Deploy to Lightsail
      uses: appleboy/ssh-action@v0.1.5
      with:
        host: ${{ secrets.LIGHTSAIL_HOST }}
        username: bitnami
        key: ${{ secrets.LIGHTSAIL_SSH_KEY }}
        script: |
          cd /opt/bitnami/apache/apps/
          sudo rm -rf *
          
    - name: Copy files via SCP
      uses: appleboy/scp-action@v0.1.4
      with:
        host: ${{ secrets.LIGHTSAIL_HOST }}
        username: bitnami
        key: ${{ secrets.LIGHTSAIL_SSH_KEY }}
        source: "."
        target: "/opt/bitnami/apache/apps/"
        
    - name: Restart Apache
      uses: appleboy/ssh-action@v0.1.5
      with:
        host: ${{ secrets.LIGHTSAIL_HOST }}
        username: bitnami
        key: ${{ secrets.LIGHTSAIL_SSH_KEY }}
        script: |
          sudo chown bitnami:bitnami /opt/bitnami/apache/apps/
          sudo chown bitnami:bitnami /opt/bitnami/apache/conf/
          sudo chown bitnami:bitnami /opt/bitnami/apache/htdocs/
          sudo chown bitnami:bitnami /opt/bitnami/apache/office/
          sudo chown bitnami:bitnami /opt/bitnami/apache/backoffice/
          sudo /opt/bitnami/ctlscript.sh restart apache